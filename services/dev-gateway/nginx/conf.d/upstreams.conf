upstream booking_api {
  least_conn;
  server bookingservice.api:80 max_fails=3 fail_timeout=10s;
  keepalive 64;
}

upstream room_api {
  least_conn;
  server room-service:8083 max_fails=3 fail_timeout=10s;
  keepalive 64;
}

upstream pricing_api {
  least_conn;
  server pricing-service:5003 max_fails=3 fail_timeout=10s;
  keepalive 64;
}

upstream payment_api {
  least_conn;
  server payment-service:80 max_fails=3 fail_timeout=10s;
  keepalive 64;
}

upstream auth_api {
  least_conn;
  server auth-service:8080 max_fails=3 fail_timeout=10s;
  keepalive 64;
}
