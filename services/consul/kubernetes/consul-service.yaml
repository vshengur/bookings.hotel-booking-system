apiVersion: v1
kind: Service
metadata:
  name: consul
  namespace: bookings
spec:
  type: LoadBalancer
  ports:
    - port: 8500
      targetPort: 8500
      protocol: TCP
      nodePort: 30500
  selector:
    app: consul
