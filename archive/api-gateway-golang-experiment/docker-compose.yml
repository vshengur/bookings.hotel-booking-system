version: '3.8'
services:
  gateway:
    build: .
    ports:
      - "8080:8080"
    environment:
      - CONSUL_HTTP_ADDR=http://consul:8500
    depends_on:
      - consul
  consul:
    image: consul:latest
    ports:
      - "8500:8500"
      - "8600:8600"
